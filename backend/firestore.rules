rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // API writes are performed by Firebase Admin SDK in Cloud Functions.
    // Direct client access can be tightened when the frontend auth flow is added.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
